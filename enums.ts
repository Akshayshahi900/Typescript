enum seatChoice{
    AISLE =22,
    MIDDLE,
    WINDOW,
}
enum foreignseatChoice{
    AISLE = "aisle",
    MIDDLE ="middle",
    WINDOW ="window",
    FOURTH = 0
}
const ak_seat = seatChoice.WINDOW
const as_seat = foreignseatChoice.FOURTH


//putting a const keyword before enum will clean the code of the js removing the iff functions and other related stuff.

export {}